FROM python:3.9
COPY . /HelloWorld
WORKDIR /HelloWorld
ENTRYPOINT ["python", "HelloWorld.py"]
# To run this container: docker run imagename --name="name"
# Example: docker run badebade/hello_world --name="Bavo"
